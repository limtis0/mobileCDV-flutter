name: mobile_cdv
description: CDV mobile app

flutter:
  uses-material-design: true
  assets:
    - assets/locale/locale.csv
    - assets/images/logo-text.png

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Additional logic
  http: ^0.13.4
  jwt_decode: ^0.3.1
  csv: ^5.0.1
  url_launcher: ^6.0.20 #offical package made by flutter если че

  # Storing data
  shared_preferences: ^2.0.13
  path_provider: ^2.0.9

  # Frontend
  table_calendar: ^3.0.4
  flutter_local_notifications: ^9.4.0
  provider: ^6.0.2
  scroll_to_index: any


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0